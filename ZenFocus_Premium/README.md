# ZenFocus — Premium Flocus-like app (original design)

This project is a full-featured premium focus timer:
- Custom original UI (not a clone)
- Pomodoro cycles, penalties detection, streaks
- Razorpay one-time purchase (serverless)
- Tailwind for styling, Framer Motion available for animations
- Next.js App Router (app/)

## Quickstart
1. `npm install`
2. Create `.env.local` using `.env.example`
3. `npm run dev`
4. Deploy to Vercel (set env vars in project settings)
